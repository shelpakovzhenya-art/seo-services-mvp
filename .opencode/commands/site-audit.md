---
description: Audit a live page and map findings to repo files
agent: build
---
Use `@CLAUDE.md` and `@.claude/commands/site-audit.md` as the source of truth.

Audit:
`$ARGUMENTS`

Use the built-in project SEO audit pipeline when useful and trace findings back to the real source files in this repository.
