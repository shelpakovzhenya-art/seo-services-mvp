---
description: Fix a known SEO issue in repo code
agent: build
---
Use `@CLAUDE.md` and `@.claude/commands/seo-fix.md` as the source of truth.

Issue or route:
`$ARGUMENTS`

Trace the issue to the real source file and fix it directly in this repository.
