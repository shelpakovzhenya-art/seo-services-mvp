providers = ["node"]

[variables]

[phases.setup]
nixPkgs = ["...", "python311"]

[phases.install]
cmds = ["...", "python3 -m pip install -r scripts/seo_audit/requirements.txt"]

[start]
cmd = "npm run start"
